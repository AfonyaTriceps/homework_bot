# Ѕот-ассистент

## ќ проекте:
Telegram-бот, который обращаетс€ к API сервиса ѕрактикум.ƒомашка и узнает статус домашней работы: 
* вз€та ли ваша домашка в ревью
* проверена ли она
* прин€л еЄ ревьюер или вернул на доработку.

### „то делает Ѕот:
* раз в 10 минут опрашивает API сервиса ѕрактикум.ƒомашка и провер€ет статус отправленной на ревью домашней работы;
* при обновлении статуса анализирует ответ API и отправл€ет соответствующее уведомление в Telegram;
* логирует свою работу и сообщает о важных проблемах сообщением в Telegram.

###  ак запустить проект:
> команды указаны дл€ Windows

 лонировать репозиторий и далее перейти в него:
<pre><code>git clone git@github.com:AfonyaTriceps/homework_bot.git</code></pre>

Cоздать и активировать виртуальное окружение:
<pre><code>/homework_bot$ python -m venv venv</code>
<code>/homework_bot$ source venv/Scripts/activate</code></pre>

”становить зависимости из файла requirements.txt:
<pre><code>pip install -r requirements.txt</code></pre>

—оздайте файл `.env` в корневой директории проекта и заполните переменные окружени€ в нем:
<pre><code>PRACTICUM_TOKEN = токен_к_api_практикум.домашка
TELEGRAM_TOKEN = токен_вашего_тг_бота
TELEGRAM_CHAT_ID = ваш_telegram_ID</code></pre>
